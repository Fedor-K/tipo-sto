# TIPO-STO - CRM для автосервиса

## Концепция проекта

### Что такое TIPO-STO

**CRM + автоматизация для СТО, работающая на базе 1С**

Заменяет "5 Систем" и Дикиди единой платформой с работающей CRM и ИИ-автоматизацией.

### Сравнение с конкурентами

| Функция | 5 Систем | Дикиди | TIPO-STO |
|---------|----------|--------|----------|
| Дашборд | ✅ | ❌ | ✅ |
| CRM | ❌ сломана | ✅ | ✅ |
| Данные из 1С | ✅ | ❌ | ✅ |
| Telegram бот | ❌ | ❌ | ✅ |
| ИИ-агенты | ❌ | ❌ | ✅ |
| IP-телефония | ❌ | ❌ | ✅ |
| Онлайн-касса | 🔗 1С | ❌ | ✅ |
| ТСД (склад) | 🔗 1С | ❌ | ✅ |
| Сложный функционал | 🔗 ссылки на 1С | ❌ | 🔗 ссылки на 1С |

### Архитектура

```
                              ┌──────────────────┐
                              │  Rent1C (облако) │
                              │   OData API      │
                              └────────┬─────────┘
                                       │
                              ┌────────▼─────────┐
                              │    TIPO-STO      │
                              │  100% Custom UI  │
                              └────────┬─────────┘
                                       │
    ┌───────────┬───────────┬──────────┼──────────┬───────────┬───────────┐
    │           │           │          │          │           │           │
┌───▼───┐ ┌─────▼─────┐ ┌───▼───┐ ┌────▼────┐ ┌───▼───┐ ┌─────▼─────┐ ┌───▼───┐
│Web UI │ │IP-телефон │ │Онлайн │ │Терминал │ │  ТСД  │ │ Telegram  │ │  ИИ   │
│       │ │           │ │ касса │ │ оплаты  │ │       │ │   бот     │ │агенты │
└───────┘ └───────────┘ └───────┘ └─────────┘ └───────┘ └───────────┘ └───────┘
```

### Интерфейс: 100% Custom UI через OData

**Решение (январь 2026):** Полностью кастомный интерфейс, работающий через Rent1C OData API.

| 100% — TIPO-STO (свой UI) | Ссылки на 1С |
|---------------------------|--------------|
| Клиенты, авто, заказы | Сложные отчёты |
| Создание заказ-нарядов | Бухгалтерия |
| Работы, запчасти, расчёты | Настройки учёта |
| Все ежедневные операции | Редкие операции |

**Почему НЕ гибридный подход (iframe + OData):**
- Тестировали встраивание 1С веб-клиента в iframe
- Проблема: требуется повторная аутентификация в каждой вкладке
- Это неприемлемо для пользователей

**Принцип:** Весь основной функционал — в кастомном UI. Только сложные/редкие операции — ссылки на 1С.

### Интеграции

| Система | Функция |
|---------|---------|
| IP-телефония | Входящий звонок → карточка клиента |
| Онлайн-касса | Чеки, отправка в ОФД |
| Терминал оплаты | Карты, СБП |
| ТСД | Склад, штрихкоды, инвентаризация |
| Telegram бот | Запись, статус ремонта, чат с клиентом |
| ИИ-агент продаж | "Пора менять масло", "Скидка на шиномонтаж" |
| ИИ-агент напоминаний | "Завтра запись в 10:00", "Машина готова" |
| ИИ-агент поддержки | Отвечает на вопросы в Telegram |

### Кто как работает

| Роль | Интерфейс |
|------|-----------|
| Приёмщик | TIPO-STO Web UI |
| Механик | TIPO-STO Web UI / планшет |
| Кладовщик | TIPO-STO + ТСД |
| Кассир | TIPO-STO + касса + терминал |
| Бухгалтер | 1С напрямую |
| Клиент | Telegram бот |

### Принципы

1. **Rent1C OData = источник данных** (облачная 1С)
2. **100% кастомный UI** — никаких iframe, только OData API
3. **Сотрудники работают через TIPO-STO, не в 1С напрямую**
4. **Сложный функционал → ссылки на веб-клиент 1С**
5. **ИИ-агенты автоматизируют рутину**

---

## Текущая архитектура

### Серверы

#### 185.222.161.252 - Продакшн 1С (источник данных)
- **Это ОСНОВНАЯ база с реальными данными**
- RDP: Администратор / (спросить пароль)
- SSH: `sshpass -p 'RhK312Sz1$' ssh 22Linia1@185.222.161.252`
- База 1С: D:\Base
- Конфигурация: Альфа-Авто:Автосалон+Автосервис+Автозапчасти
- API Gateway: http://185.222.161.252:8080
- Файл API: `C:\api_gateway_v3.py`
- **ВАЖНО:** API Gateway требует 32-bit Python для COM-коннектора 1С!
  ```bash
  # Запуск API Gateway (на сервере 185.222)
  C:\Python311-32\python.exe api_gateway_v3.py
  ```
  Если ошибка "Класс не зарегистрирован" — перезапустить с 32-bit Python.

#### 198.12.73.168 - TIPO-STO (FastAPI приложение)
- SSH: root / S5M50Xb9Os2a8GvgUs (порт 22)
- Ubuntu 24.04 LTS, Python 3.12, 2.5GB RAM
- RackNerd KVM VPS
- TIPO-STO работает на порту 8000
- AI proxy на порту 8080
- UI: http://198.12.73.168:8000/ui
- Mechanic: http://198.12.73.168:8000/mechanic
- Путь: /root/tipoSTO/ (venv в /root/tipoSTO/venv/)

### Rent1C (облачная 1С) - ОСНОВНОЙ ИСТОЧНИК ДАННЫХ
> **Решение (январь 2026):** Используем Rent1C как основную базу данных.
> Доступ через OData API, 100% кастомный интерфейс.

- Аккаунт: 1R96614U1 / X7gDhIChmV
- OData URL: `https://aclient.1c-hosting.com/1R96614/1R96614_AA61AS_e771ys34or/odata/standard.odata`
- Веб-клиент: `https://aclient.1c-hosting.com/1R96614/1R96614_AA61AS_e771ys34or`
- Авторизация OData: `Администратор` / пустой пароль
- Статус: **АКТИВНЫЙ**

---

## АНАЛИЗ БАЗЫ 185.222 (ПРОДАКШН)

### Общий объём данных

| Категория | Количество объектов | Всего записей |
|-----------|---------------------|---------------|
| Справочники (Catalogs) | 121 | 1,375,712 |
| Документы (Documents) | 134 | 160,250 |
| **ИТОГО** | **255** | **1,535,962** |

### Ключевые справочники

| Справочник | Записей | Примечание |
|------------|---------|------------|
| Номенклатура | 679,249 | Самый большой! |
| ЕдиницыИзмерения | 663,849 | Много записей |
| ДоговорыВзаиморасчетов | 12,419 | |
| Автомобили | 8,288 | |
| Контрагенты | 7,675 | Клиенты |
| Автоработы | 2,935 | |
| Модели | 1,102 | |
| Цвета | 70 | |
| Сотрудники | 66 | |
| ПодразделенияКомпании | 14 | |
| СкладыКомпании | 8 | |
| ВидыСостоянийЗаказНарядов | 8 | |
| Цеха | 6 | |
| ТипыЦен | 6 | |
| Организации | 4 | |
| ВидыРемонта | 4 | |
| СтавкиНДС | 4 | |
| Валюты | 2 | |

### Ключевые документы

| Документ | Записей | Табличные части |
|----------|---------|-----------------|
| ПоступлениеТоваров | 41,987 | 2 |
| ПриходныйКассовыйОрдер | 35,766 | 1 |
| ЗаказНаряд | 27,954 | 5 |
| РеализацияТоваров | 27,130 | 2 |
| РасходныйКассовыйОрдер | 20,672 | 1 |
| ЗаявкаНаРемонт | 6,741 | - |

### Структура ЗаказНаряд (главный документ)

**Шапка:** 71 поле

**Табличные части:**
1. **Товары** - 24 поля (Номенклатура, Количество, Цена, Сумма...)
2. **Работы** - 25 полей (Работа, Исполнитель, НормаВремени...)
3. **Исполнители** - 4 поля
4. **Материалы** - 9 полей
5. **МатериалыЗаказчика** - 7 полей
6. **Автомобили** - 2 поля (LineNumber, Автомобиль_Key) - связь с авто

**Ключевые ссылки в шапке:**
- Организация_Key
- Контрагент_Key — **плательщик** (не заказчик!)
- ДоговорВзаиморасчетов_Key
- Цех_Key
- Мастер_Key (Сотрудники)
- ТипЦен_Key
- ВидРемонта_Key
- Состояние_Key
- ВалютаДокумента_Key
- ДокументОснование — НЕ для ЗаявкаНаРемонт! (только СводныйРемонтныйЗаказ, ЗаказПокупателя, ЗаказНаАвтомобиль)

**⚠️ ВАЖНО: Workflow документов в Альфа-Авто**

### Этапы обслуживания клиента

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   ЗАЯВКА    │───▶│   ЗАПИСАН   │───▶│  В РАБОТЕ   │───▶│  ВЫПОЛНЕН   │
│  (контакт)  │    │(план ремонта│    │   (заезд    │    │  (закрытие) │
│             │    │   до заезда)│    │   авто)     │    │             │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
       │                  │                  │
       ▼                  ▼                  ▼
  ЗаявкаНаРемонт    ЗаявкаНаРемонт     ЗаказНаряд
  (первичная)       (с работами)    (на основании)
```

### Документы и их назначение

| Документ | Когда используется | Ключевые особенности |
|----------|-------------------|---------------------|
| `ЗаявкаНаРемонт` | **ДО заезда авто** — запись, планирование, согласование | Есть `Заказчик_Key`, `Автомобиль_Key`, Работы, Товары |
| `ЗаказНаряд` | **ПОСЛЕ заезда авто** — фактический ремонт, оплата, закрытие | НЕТ `Заказчик_Key`, `Автомобиль_Key` — только табл.часть |

### Отличие реквизитов

| Реквизит | ЗаявкаНаРемонт | ЗаказНаряд |
|----------|----------------|------------|
| `Заказчик_Key` | ✅ ЕСТЬ | ❌ НЕТ в OData! |
| `Автомобиль_Key` | ✅ ЕСТЬ | ❌ НЕТ (только табл.часть `Автомобили`) |
| `Контрагент_Key` | ✅ ЕСТЬ | ✅ ЕСТЬ (плательщик) |
| `Автоработы` | ✅ ЕСТЬ | ✅ ЕСТЬ |
| `Товары` | ✅ ЕСТЬ | ✅ ЕСТЬ |
| `Исполнители` | ✅ ЕСТЬ | ✅ ЕСТЬ |

### Механизм "Ввести на основании"

В интерфейсе 1С при операции "Ввести на основании → ЗаказНаряд":
1. Данные из `ЗаявкаНаРемонт` (Заказчик, Автомобиль, Работы) копируются в форму
2. Форма `ЗаказНаряда` отображает их как **вычисляемые реквизиты**
3. Поле `ДокументОснование` НЕ может содержать `ЗаявкаНаРемонт` (только `СводныйРемонтныйЗаказ`, `ЗаказПокупателя`, `ЗаказНаАвтомобиль`)

**Через OData этот механизм недоступен!**

### Проблема при создании ЗаказНаряда через OData

- Поля "Заказчик" и "Автомобиль" на форме показывают **"Объект не найден"**
- Это НЕ ошибка — данные есть в `Контрагент_Key` и табличной части `Автомобили`
- Форма не может их отобразить без внутреннего механизма связи

### Решение для TIPO-STO (январь 2026)

**Работаем напрямую с `ЗаказНаряд`:**

Поскольку все сотрудники работают через TIPO-STO (не через интерфейс 1С), проблема с "Объект не найден" **не актуальна**:

- TIPO-STO отображает Заказчика и Автомобиль правильно (из API)
- Данные хранятся в 1С: `Контрагент_Key` + табличная часть `Автомобили`
- Форму 1С никто не открывает — 1С только как база данных

**Workflow:**
1. `ЗаказНаряд` — основной рабочий документ для ремонта
2. `ЗаявкаНаРемонт` — опционально, для предварительных записей/планирования

**При создании ЗаказНаряда через API передаём:**
- `Контрагент_Key` — клиент (он же заказчик, он же плательщик)
- `Автомобили[]` — табличная часть с автомобилем
- `Автоработы[]`, `Товары[]`, `Исполнители[]` — как обычно

---

## ПЛАН ПЕРЕНОСА 185.222 → Rent1C (АРХИВ)

> **Примечание:** Этот план был составлен ранее. Сейчас работаем напрямую с Rent1C.
> Раздел оставлен для справки если понадобится синхронизация данных.

### Порядок переноса (по зависимостям)

**ЭТАП 1: Базовые справочники (без зависимостей)**
- Валюты, ЕдиницыИзмерения, СтавкиНДС, Организации
- Цвета, Модели, ТипыКузовов, ТипыДвигателей, ТипыКПП
- ВидыРемонта, ВидыСостоянийЗаказНарядов, Должности, Нормочасы

**ЭТАП 2: Справочники с простыми зависимостями**
- ТипыЦен → Валюты
- Цеха → Организации
- ПодразделенияКомпании → Организации
- СкладыКомпании → Организации, Подразделения
- Сотрудники → Должности, Подразделения
- Автоработы → ЕдиницыИзмерения

**ЭТАП 3: Основные справочники**
- Контрагенты (7,675 записей)
- Номенклатура (679,249 записей!)

**ЭТАП 4: Связанные справочники**
- Автомобили → Контрагенты, Модели, Цвета
- ДоговорыВзаиморасчетов → Контрагенты, Организации, Валюты

**ЭТАП 5: Документы**
- ЗаказНаряд → ВСЕ справочники + табличные части
- РеализацияТоваров, ПоступлениеТоваров
- ПриходныйКассовыйОрдер, РасходныйКассовыйОрдер

### Оценка времени через OData

| Этап | Записей | Время |
|------|---------|-------|
| Этап 1 (базовые) | ~800 | 1.5 мин |
| Этап 2 (зависимые) | ~200 | 0.5 мин |
| Этап 3 (основные) | 687,000 | **19 часов** |
| Этап 4 (связанные) | 20,700 | 35 мин |
| Этап 5 (документы) | 160,000 | **4.5 часа** |
| **ИТОГО** | **~870,000** | **~24 часа** |

### Варианты переноса

| Вариант | Описание | Время | Риски |
|---------|----------|-------|-------|
| **A** | Полный перенос через OData | ~24 часа | Высокие |
| **B** | Только справочники через OData | ~20 часов | Средние |
| **C** | Выгрузка/Загрузка XML через 1С | 2-4 часа | Низкие |
| **D** | Копирование .dt файла | 30 мин | Низкие |

**Рекомендация:** Вариант C или D (стандартные механизмы 1С)

### Сложности переноса документов

1. **Табличные части** - OData не всегда поддерживает их создание
2. **Ref_Key маппинг** - нужно хранить соответствие GUID старой и новой базы
3. **Битые ссылки** - в исходной базе есть пустые поля (например, Поставщик у Автомобилей)
4. **Проведение документов** - бизнес-логика может отличаться
5. **Нумерация** - новая база может генерировать другие номера

---

## API Gateway (185.222.161.252:8080)

### Эндпоинты метаданных

```
GET /api/metadata/catalogs          - Список всех справочников
GET /api/metadata/documents         - Список всех документов
GET /api/metadata/catalog/{name}/fields   - Поля справочника
GET /api/metadata/document/{name}/fields  - Поля документа
GET /api/metadata/document/{name}/tabular - Табличные части документа
GET /api/stats/catalog/{name}       - Количество записей в справочнике
GET /api/stats/document/{name}      - Количество записей в документе
```

### Эндпоинты данных

```
GET /api/catalog/{name}             - Записи справочника (?top=100)
GET /api/document/{name}            - Записи документа (?top=100)
GET /api/catalog/{name}/{ref_key}   - Конкретная запись
```

---

## Текущий статус

### Решение: TIPO-STO → Rent1C OData (январь 2026)

- **Rent1C = основная база** — облачная 1С с OData API
- **100% кастомный UI** — гибридный подход (iframe) отвергнут
- **Причина отказа от iframe**: повторная аутентификация в каждой вкладке
- **Демо работает**: `demo_v2.html` + `test_server.py`

### Важные технические детали

**Связь Автомобиль ↔ Клиент:**
- В справочнике `Catalog_Автомобили` НЕТ прямой ссылки на владельца
- Связь определяется через историю заказов: `Document_ЗаказНаряд` → табличная часть `Автомобили`
- Для получения машин клиента нужно:
  1. Найти все заказы клиента: `Document_ЗаказНаряд?$filter=Контрагент_Key eq guid'...'`
  2. Для каждого заказа получить табличную часть: `Document_ЗаказНаряд(guid'...')/Автомобили`
  3. Собрать уникальные `Автомобиль_Key`
  4. Загрузить данные машин из `Catalog_Автомобили`
- Для новых клиентов без истории — выбор из полного справочника

**Исполнители в заказ-нарядах:**

Исполнители (механики) назначаются на работы через табличную часть `Исполнители` документа `ЗаказНаряд`.

Структура записи исполнителя:
```json
{
  "LineNumber": "1",
  "ИдентификаторРаботы": "1",      // Связь с работой в табл.части Автоработы
  "Исполнитель_Key": "guid",        // Ссылка на Catalog_Сотрудники
  "Цех_Key": "guid",                // ОБЯЗАТЕЛЬНО! Иначе ошибка записи
  "Процент": 100                    // Процент участия (100 = один исполнитель)
}
```

Требования к сотруднику для назначения исполнителем:
1. `Исполнитель = true` — флаг "является исполнителем"
2. `УчаствуетВПланировании = "ПланированиеПроизводства"` — участвует в планировании
3. `Цех_Key` — назначен цех
4. `ТипРесурса_Key` — указан тип ресурса

**Важно про НормыВремени:**
- В интерфейсе 1С исполнители показываются ТОЛЬКО для работ с заполненной табл.частью `НормыВремени`
- Большинство работ (~3000) не имеют норм → список исполнителей пуст
- Через OData API это ограничение НЕ действует — можно назначить исполнителя на любую работу
- Для исправления в 1С нужно массово добавить `НормыВремени` для всех работ

Пример работы с нормами (исполнители доступны):
```
Тест работа с нормами:
  СпособРасчетаСтоимостиРаботы: ПоНормеВремени
  ВремяВыполнения: 60
  НормыВремени: [{"LineNumber": "1", "ВремяВыполнения": 30}]
```

Пример работы без норм (исполнители НЕ доступны в UI 1С):
```
Автомобиль - антикор:
  СпособРасчетаСтоимостиРаботы: ПоНормеВремени
  ВремяВыполнения: 10
  НормыВремени: []  ← пустая табличная часть
```

**Статус базы 1С (январь 2026):**
- Сотрудников-исполнителей: **69 из 69** (все настроены)
- Работ с НормыВремени: **83 из 105**
- Все сотрудники и работы исправлены через OData API

**Заявка на ремонт (Document_ЗаявкаНаРемонт) - основной документ:**

Для правильного отображения Заказчика и Автомобиля в списках 1С используется `Document_ЗаявкаНаРемонт`, а не `Document_ЗаказНаряд`.

### Поля шапки документа

**Обязательные поля (без них документ не создастся):**

| Поле | Тип | Описание | Источник |
|------|-----|----------|----------|
| `Date` | DateTime | Дата документа | Текущее время |
| `Posted` | Boolean | Проведён | `true` |
| `Организация_Key` | GUID | Организация | DEFAULTS["org"] |
| `ПодразделениеКомпании_Key` | GUID | Подразделение | DEFAULTS["division"] |
| `Заказчик_Key` | GUID | Клиент (контрагент) | Выбор пользователя |
| `ВидРемонта_Key` | GUID | Вид ремонта | DEFAULTS["repair_type"] |
| `Цех_Key` | GUID | Цех | DEFAULTS["workshop"] |
| `ТипЦен_Key` | GUID | Тип цен товаров | DEFAULTS["price_type"] |
| `ТипЦенРабот_Key` | GUID | Тип цен работ | DEFAULTS["price_type"] |
| `ВалютаДокумента_Key` | GUID | Валюта | DEFAULTS["currency"] |
| `КурсДокумента` | Number | Курс валюты | `1` |
| `Автор_Key` | GUID | Автор документа | DEFAULTS["author"] |
| `Состояние` | String | Состояние | `"НеУказано"` |

**Автозаполняемые поля (из справочников):**

| Поле | Тип | Описание | Источник |
|------|-----|----------|----------|
| `Автомобиль_Key` | GUID | Автомобиль | Выбор пользователя |
| `Контрагент_Key` | GUID | Плательщик | = Заказчик_Key (по умолчанию) |
| `VIN` | String | VIN-код авто | Catalog_Автомобили.VIN |
| `ГосНомер` | String | Гос. номер (ЛАТИНИЦА!) | Ввод пользователя + конвертация |
| `Модель_Key` | GUID | Модель авто | Catalog_Автомобили.Модель_Key |
| `ГодВыпуска` | DateTime | Год выпуска | Catalog_Автомобили.ГодВыпуска |
| `ДоговорВзаиморасчетов_Key` | GUID | Договор клиента | Catalog_ДоговорыВзаиморасчетов |
| `Мастер_Key` | GUID | Мастер | DEFAULTS["master"] или выбор |
| `ДатаНачала` | DateTime | Дата начала работ | Сегодня 09:00 |
| `ДатаОкончания` | DateTime | Дата окончания работ | Сегодня 18:00 |
| `ПредставлениеТелефона` | String | Телефон клиента | КонтактнаяИнформация |
| `ПредставлениеТелефонаСтрокой` | String | Телефон (дубль) | КонтактнаяИнформация |
| `АдресЭлектроннойПочты` | String | Email клиента | КонтактнаяИнформация |
| `АдресЭлектроннойПочтыСтрокой` | String | Email (дубль) | КонтактнаяИнформация |
| `Пробег` | String | Пробег авто | Ввод пользователя |
| `ОписаниеПричиныОбращения` | String | Комментарий | Ввод пользователя |

**Поля которых НЕТ в документе (только в справочнике Автомобили):**
- `Цвет_Key`, `ТипКузова_Key`, `ТипДвигателя_Key`, `ТипКПП_Key` — эти поля есть только в `Catalog_Автомобили`

### Табличные части

**1. ПричиныОбращения (обязательно для связи работ):**

| Поле | Тип | Описание |
|------|-----|----------|
| `LineNumber` | String | Номер строки ("1") |
| `ИдентификаторПричиныОбращения` | String | ID для связи с работами ("1") |
| `ПричинаОбращения_Key` | GUID | Причина (Ремонт = 7d9f8933-1a7f-11e6-bee5-20689d8f1e0d) |
| `ПричинаОбращенияСодержание` | String | Текст причины |
| `ВидРемонтаПричиныОбращения_Key` | GUID | Вид ремонта |

**2. Автоработы:**

| Поле | Тип | Описание |
|------|-----|----------|
| `LineNumber` | String | Номер строки |
| `Авторабота_Key` | GUID | Ссылка на Catalog_Автоработы |
| `ИдентификаторРаботы` | String | ID работы (для связи с исполнителями) |
| `ИдентификаторПричиныОбращения` | String | Связь с ПричиныОбращения |
| `Количество` | Number | Количество |
| `Коэффициент` | Number | `0` |
| `Цена` | Number | Цена за единицу |
| `Сумма` | Number | Количество × Цена |
| `СуммаВсего` | Number | Итоговая сумма (= Сумма) |
| `СпособРасчетаСтоимостиРаботы` | String | `"ФиксированнойСуммой"` |

**3. Товары:**

**КРИТИЧЕСКИ ВАЖНО:** `СтавкаНДС_Key` ДОЛЖЕН быть пустым GUID `00000000-0000-0000-0000-000000000000`, иначе документ не запишется!

| Поле | Тип | Описание |
|------|-----|----------|
| `LineNumber` | String | Номер строки |
| `Номенклатура_Key` | GUID | Ссылка на Catalog_Номенклатура |
| `ЕдиницаИзмерения_Key` | GUID | **ОБЯЗАТЕЛЬНО!** DEFAULTS["unit"] |
| `Количество` | Number | Количество |
| `Коэффициент` | Number | `1` |
| `Цена` | Number | Цена за единицу |
| `Сумма` | Number | Количество × Цена |
| `СтавкаНДС_Key` | GUID | **ПУСТОЙ GUID!** `00000000-0000-0000-0000-000000000000` |
| `СуммаНДС` | Number | `0` |
| `СуммаВсего` | Number | Итоговая сумма |
| `СкладКомпании_Key` | GUID | Склад (DEFAULTS["warehouse"]) |
| `ИдентификаторПричиныОбращения` | String | Связь с ПричиныОбращения (просто `"1"`) |
| `ПроцентСкидки` | Number | `0` |
| `СуммаСкидки` | Number | `0` |
| `ХарактеристикаНоменклатуры_Key` | GUID | Пустой GUID |
| `СкидкаНаТовар_Key` | GUID | Пустой GUID |
| `ЗначениеСкидкиШапкиИзменено` | Boolean | `false` |
| `ЗначениеСкидкиСтрокиИзменено` | Boolean | `false` |

**Рабочий пример Товары (проверено 25.01.2026):**
```json
{
  "LineNumber": "1",
  "Номенклатура_Key": "41b8a979-18fc-11e6-a20f-6cf049a63e1b",
  "Количество": 2,
  "ЕдиницаИзмерения_Key": "6ceca65d-18f4-11e6-a20f-6cf049a63e1b",
  "Коэффициент": 1,
  "Цена": 500,
  "Сумма": 1000,
  "СтавкаНДС_Key": "00000000-0000-0000-0000-000000000000",
  "СуммаНДС": 0,
  "СуммаВсего": 1000,
  "СкладКомпании_Key": "65ce4049-fa7c-11e5-9841-6cf049a63e1b",
  "ИдентификаторПричиныОбращения": "1",
  "ПроцентСкидки": 0,
  "СуммаСкидки": 0,
  "ХарактеристикаНоменклатуры_Key": "00000000-0000-0000-0000-000000000000",
  "СкидкаНаТовар_Key": "00000000-0000-0000-0000-000000000000",
  "ЗначениеСкидкиШапкиИзменено": false,
  "ЗначениеСкидкиСтрокиИзменено": false
}
```

**4. ВспомогательныеАвтоработы (упрощённая структура):**

| Поле | Тип | Описание |
|------|-----|----------|
| `LineNumber` | String | Номер строки |
| `Авторабота_Key` | GUID | Ссылка на Catalog_Автоработы |
| `ИдентификаторРаботы` | String | ID работы |
| `НормаВремени` | Number | Норма времени |

**ВАЖНО:** Не все работы подходят для ВспомогательныеАвтоработы — только определённого типа.

**5. Исполнители:**

| Поле | Тип | Описание |
|------|-----|----------|
| `LineNumber` | String | Номер строки |
| `ИдентификаторРаботы` | String | Связь с Автоработы.ИдентификаторРаботы |
| `Исполнитель_Key` | GUID | Ссылка на Catalog_Сотрудники |
| `Цех_Key` | GUID | **ОБЯЗАТЕЛЬНО!** Цех исполнителя |
| `Процент` | Number | Процент участия (100 = один исполнитель) |

### Особенности полей

**ГосНомер — конвертация кириллицы:**
- 1C OData API НЕ принимает кириллицу — возвращает ошибку "Не удалось записать"
- Код автоматически конвертирует: `К456ОР199` → `K456OP199`
- Таблица по ГОСТ: А→A, В→B, Е→E, К→K, М→M, Н→H, О→O, Р→P, С→C, Т→T, У→Y, Х→X
- Fallback: Б→B, И→I, Г→G, Л→L, Д→D, Ж→J

**ГодВыпуска — формат DateTime:**
- Передаётся как `"2020-01-01T00:00:00"`, НЕ как число `2020`
- Берётся напрямую из `Catalog_Автомобили.ГодВыпуска`

**ЕдиницаИзмерения_Key — обязательно для Товаров:**
- Без этого поля документ не создастся
- Используется DEFAULTS["unit"] = `"6ceca65d-18f4-11e6-a20f-6cf049a63e1b"` (шт)

**Цех_Key — обязательно для Исполнителей:**
- Без указания цеха исполнитель не будет привязан к работе

### Пример создания через OData

```json
{
  "Date": "2026-01-25T12:00:00",
  "Posted": true,
  "Организация_Key": "39b4c1f1-fa7c-11e5-9841-6cf049a63e1b",
  "ПодразделениеКомпании_Key": "39b4c1f0-fa7c-11e5-9841-6cf049a63e1b",
  "Заказчик_Key": "guid-клиента",
  "Контрагент_Key": "guid-клиента",
  "Автомобиль_Key": "guid-авто",
  "VIN": "WBAKJ810X0C123456",
  "ГосНомер": "K456OP199",
  "Модель_Key": "guid-модели",
  "ГодВыпуска": "2020-01-01T00:00:00",
  "ДоговорВзаиморасчетов_Key": "guid-договора",
  "Мастер_Key": "guid-мастера",
  "ДатаНачала": "2026-01-25T09:00:00",
  "ДатаОкончания": "2026-01-25T18:00:00",
  "Пробег": "95000",
  "ВидРемонта_Key": "7d9f8931-1a7f-11e6-bee5-20689d8f1e0d",
  "Цех_Key": "65ce404a-fa7c-11e5-9841-6cf049a63e1b",
  "ТипЦен_Key": "65ce4042-fa7c-11e5-9841-6cf049a63e1b",
  "ТипЦенРабот_Key": "65ce4042-fa7c-11e5-9841-6cf049a63e1b",
  "ВалютаДокумента_Key": "6bd1932d-fa7c-11e5-9841-6cf049a63e1b",
  "КурсДокумента": 1,
  "Автор_Key": "39b4c1f2-fa7c-11e5-9841-6cf049a63e1b",
  "ОписаниеПричиныОбращения": "Замена масла",
  "Состояние": "НеУказано",
  "ПричиныОбращения": [{
    "LineNumber": "1",
    "ИдентификаторПричиныОбращения": "1",
    "ПричинаОбращения_Key": "7d9f8933-1a7f-11e6-bee5-20689d8f1e0d",
    "ПричинаОбращенияСодержание": "Замена масла",
    "ВидРемонтаПричиныОбращения_Key": "7d9f8931-1a7f-11e6-bee5-20689d8f1e0d"
  }],
  "Автоработы": [{
    "LineNumber": "1",
    "Авторабота_Key": "guid-работы",
    "ИдентификаторРаботы": "1",
    "ИдентификаторПричиныОбращения": "1",
    "Количество": 1,
    "Коэффициент": 0,
    "Цена": 1500,
    "Сумма": 1500,
    "СуммаВсего": 1500,
    "СпособРасчетаСтоимостиРаботы": "ФиксированнойСуммой"
  }],
  "Товары": [{
    "LineNumber": "1",
    "Номенклатура_Key": "guid-товара",
    "ЕдиницаИзмерения_Key": "6ceca65d-18f4-11e6-a20f-6cf049a63e1b",
    "ИдентификаторПричиныОбращения": "1",
    "Количество": 1,
    "Коэффициент": 1,
    "Цена": 500,
    "Сумма": 500,
    "СуммаВсего": 500,
    "СкладКомпании_Key": "65ce4049-fa7c-11e5-9841-6cf049a63e1b"
  }]
}
```

### Типичные ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| "Не удалось записать: Заявка на ремонт!" | Кириллица в ГосНомер | Конвертировать в латиницу |
| "Не удалось записать" | Нет ЕдиницаИзмерения_Key в Товарах | Добавить DEFAULTS["unit"] |
| "Не удалось записать" | **СтавкаНДС_Key не пустой GUID** | **Использовать `00000000-0000-0000-0000-000000000000`** |
| "Не удалось записать" | Несуществующий GUID работы/товара | Проверить GUID в справочнике |
| Пустой список работ в 1С | Нет ПричиныОбращения | Добавить табл.часть ПричиныОбращения |
| Работы без связи с причиной | Нет ИдентификаторПричиныОбращения | Добавить поле в Автоработы/Товары |
| "Не удалось разобрать строку как DateTime" | ГодВыпуска как "2020" | Передавать "2020-01-01T00:00:00" |

---

## Декомпозиция для внедрения

### Модули системы

```
TIPO-STO
├── 1. ЯДРО (Backend + API Gateway)
├── 2. WEB UI (интерфейс сотрудников)
├── 3. TELEGRAM БОТ (интерфейс клиентов)
├── 4. ИИ-АГЕНТЫ (автоматизация)
├── 5. ИНТЕГРАЦИИ (оборудование)
└── 6. ССЫЛКИ НА 1С (сложный функционал)
```

### МОДУЛЬ 1: Ядро (Backend + API Gateway)

| # | Задача | Результат | Приоритет |
|---|--------|-----------|-----------|
| 1.1 | API: чтение клиентов (с телефоном) | Список клиентов с ФИО, телефоном, ИНН | Высокий |
| 1.2 | API: чтение заказ-нарядов (с товарами/работами) | Полная информация по заказу включая строки | Высокий |
| 1.3 | API: поиск (клиенты, авто, заказы) | Быстрый поиск по имени, VIN, номеру заказа | Высокий |
| 1.4 | API: создание клиента | Новый клиент появляется в 1С | Высокий |
| 1.5 | API: создание автомобиля | Новый авто привязан к клиенту в 1С | Высокий |
| 1.6 | API: создание заказ-наряда | Заказ создан в 1С со всеми реквизитами | Высокий |
| 1.7 | API: обновление заказ-наряда | Изменение статуса, комментария в 1С | Средний |
| 1.8 | API: добавление работ/товаров в заказ | Строки добавлены в табличные части 1С | Средний |
| 1.9 | Аутентификация и авторизация | Только авторизованные сотрудники имеют доступ | Средний |
| 1.10 | Логирование и мониторинг | Видно кто что делал, ошибки отслеживаются | Низкий |

### МОДУЛЬ 2: Web UI (интерфейс сотрудников)

| # | Задача | Результат | Приоритет |
|---|--------|-----------|-----------|
| 2.1 | Дашборд | Сотрудник видит: заказы сегодня, в работе, сумма выручки | Высокий |
| 2.2 | Список клиентов | Поиск клиента за 2 секунды, фильтры по алфавиту | Высокий |
| 2.3 | Карточка клиента | Вся история клиента: авто, заказы, суммы | Высокий |
| 2.4 | Список заказ-нарядов | Фильтр по статусу/дате, быстрый доступ к заказу | Высокий |
| 2.5 | Карточка заказ-наряда | Все детали: работы, запчасти, исполнители, суммы | Высокий |
| 2.6 | Создание клиента | Новый клиент за 30 секунд без входа в 1С | Высокий |
| 2.7 | Создание автомобиля | Добавление авто клиенту за 30 секунд | Высокий |
| 2.8 | Создание заказ-наряда | Приёмщик создаёт заказ за 1 минуту | Высокий |
| 2.9 | Добавление работ | Выбор работ из справочника, автоподстановка цены | Высокий |
| 2.10 | Добавление товаров | Выбор запчастей, проверка остатков, автоцена | Высокий |
| 2.11 | Назначение исполнителей | Механик видит свои задачи, учёт выработки | Средний |
| 2.12 | Смена статуса заказа | Один клик: Заявка → В работе → Готов → Закрыт | Высокий |
| 2.13 | Приём оплаты | Кассир принимает оплату, чек пробивается автоматически | Средний |
| 2.14 | Печать документов | Печать заказ-наряда и акта для клиента | Средний |
| 2.15 | Адаптивная версия | Механик работает с планшета в цеху | Низкий |

### МОДУЛЬ 3: Telegram бот (интерфейс клиентов)

| # | Задача | Результат | Приоритет |
|---|--------|-----------|-----------|
| 3.1 | Регистрация клиента | Клиент привязывает Telegram по телефону | Высокий |
| 3.2 | Просмотр автомобилей | Клиент видит список своих авто с VIN | Высокий |
| 3.3 | История заказов | Клиент видит все визиты, что делали, сколько стоило | Высокий |
| 3.4 | Статус ремонта | Клиент видит в реальном времени: "В работе" / "Готов" | Высокий |
| 3.5 | Запись на сервис | Клиент выбирает дату/время, заявка падает в 1С | Высокий |
| 3.6 | Уведомление "Готово" | Клиент получает push: "Ваш авто готов" | Высокий |
| 3.7 | Напоминание о записи | За день: "Завтра в 10:00 ждём вас" | Высокий |
| 3.8 | Чат с менеджером | Клиент пишет → менеджер отвечает из TIPO-STO | Средний |
| 3.9 | Оплата онлайн | Клиент оплачивает счёт в Telegram | Низкий |
| 3.10 | Отзыв после визита | Клиент ставит оценку, отзыв сохраняется | Низкий |

### МОДУЛЬ 4: ИИ-агенты (автоматизация)

| # | Задача | Результат | Приоритет |
|---|--------|-----------|-----------|
| 4.1 | Напоминание о записи | Автоматический SMS/Telegram за день до визита | Высокий |
| 4.2 | Уведомление "Готово" | Автосообщение при смене статуса заказа | Высокий |
| 4.3 | Напоминание о ТО | Система считает когда пора на ТО и пишет клиенту | Средний |
| 4.4 | Сезонные предложения | Авторассылка: "Шиномонтаж со скидкой" | Средний |
| 4.5 | Персональные рекомендации | ИИ анализирует историю и предлагает услуги | Низкий |
| 4.6 | Ответы на вопросы | Бот отвечает на частые вопросы без менеджера | Средний |
| 4.7 | Реактивация клиентов | Система находит "спящих" и предлагает вернуться | Низкий |

### МОДУЛЬ 5: Интеграции (оборудование)

| # | Задача | Результат | Приоритет |
|---|--------|-----------|-----------|
| 5.1 | Входящий звонок → карточка | При звонке всплывает карточка клиента | Средний |
| 5.2 | Click-to-call | Клик по номеру → звонок через IP-телефонию | Средний |
| 5.3 | Пробитие чека | При закрытии заказа чек пробивается автоматически | Средний |
| 5.4 | Чек клиенту | Электронный чек на email/SMS | Средний |
| 5.5 | Приём карт | Оплата картой, сумма подтягивается из заказа | Средний |
| 5.6 | Оплата по СБП | Клиент оплачивает по QR-коду | Низкий |
| 5.7 | Приёмка товара | Кладовщик сканирует ТСД → товар в 1С | Низкий |
| 5.8 | Отгрузка/списание | Сканирование при выдаче запчастей | Низкий |
| 5.9 | Инвентаризация | Пересчёт склада через ТСД | Низкий |

### МОДУЛЬ 6: Ссылки на 1С

| # | Задача | Результат | Приоритет |
|---|--------|-----------|-----------|
| 6.1 | Меню ссылок | Раздел "Расширенные функции" со ссылками | Средний |
| 6.2 | Открытие 1С | Клик → нужная форма 1С в новой вкладке | Средний |
| 6.3 | Список ссылок | Отчёты, бухгалтерия, настройки цен | Средний |

---

## Этапы внедрения

### ЭТАП 1: MVP (4-6 недель)

**Задачи:** 1.1-1.6, 2.1-2.8, 3.1-3.7

**Результат:** Можно полностью работать через TIPO-STO
- Приёмщик создаёт заказы в TIPO-STO (не в 1С)
- Клиенты записываются через Telegram
- Клиенты видят статус ремонта онлайн
- **Отказ от Дикиди**

### ЭТАП 2: Автоматизация (2-4 недели)

**Задачи:** 2.9-2.12, 4.1-4.4, 4.6

**Результат:** Система сама коммуницирует с клиентами
- Напоминания автоматически
- Клиенты возвращаются без обзвона
- Рост повторных визитов

### ЭТАП 3: Интеграции (2-4 недели)

**Задачи:** 2.13-2.14, 5.1-5.5

**Результат:** Полный цикл в одной системе
- Звонок → карточка автоматически
- Оплата → чек автоматически
- Не нужно переключаться между программами

### ЭТАП 4: Расширение (по необходимости)

**Задачи:** 5.6-5.9, 4.5, 4.7, 6.1-6.3

**Результат:** Дополнительные возможности
- Склад через ТСД
- Продвинутый ИИ
- Доступ к сложным функциям 1С

---

## Сводка по внедрению

| Этап | Срок | Задач | Результат |
|------|------|-------|-----------|
| **MVP** | 4-6 нед | 21 | Работа через TIPO-STO, Telegram для клиентов |
| **Автоматизация** | 2-4 нед | 9 | ИИ-напоминания, авто-коммуникации |
| **Интеграции** | 2-4 нед | 7 | Касса, телефония, терминал |
| **Расширение** | по запросу | 10 | ТСД, продвинутый ИИ |
| **ИТОГО** | **2-3 мес** | **47** | **Полная замена 5 Систем + Дикиди** |

---

### Команда для деплоя на 198.12.73.168:
```bash
# Загрузить файлы
sshpass -p 'S5M50Xb9Os2a8GvgUs' scp -o StrictHostKeyChecking=no -r app/ root@198.12.73.168:/root/tipoSTO/app/

# Перезапустить
sshpass -p 'S5M50Xb9Os2a8GvgUs' ssh -o StrictHostKeyChecking=no root@198.12.73.168 "pkill -f 'uvicorn app.main'; sleep 1; cd /root/tipoSTO && nohup /root/tipoSTO/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /root/tipoSTO/app.log 2>&1 &"
```

## Миграция данных 185.222 → Rent1C (январь 2026)

### Выполненная миграция

Все легаси данные из продакшн базы 185.222.161.252 перенесены в Rent1C:

| Данные | Количество | Статус | Источник |
|--------|------------|--------|----------|
| **Работы (Автоработы)** | 2,915 | ✅ Импортировано | `Catalog.Автоработы` |
| **Цены на работы** | 2,495 | ✅ Извлечено | `Document.ЗаказНаряд.Работы` |
| **Номенклатура** | 679,216 | ✅ Выгружено | `Catalog.Номенклатура` |
| **Номенклатура с ценами** | 14,406 | ✅ Импортировано | Фильтр по наличию цен |
| **Цены на запчасти** | 50,000 | ✅ Выгружено | `InformationRegister.Цены` |
| **Клиенты** | 7,633 | ✅ Мигрировано | `Catalog.Контрагенты` (26.01.2026: +1,688 клиентов) |
| **Связи клиент↔авто** | 4,236 | ✅ Маппинг | `client_cars_mapping.json` |
| **История заказов** | 26,434 | ✅ Экспорт | `order_history.json` |
| **Детали заказов** | 25,185 | ✅ Экспорт | `order_details.json` (60K работ + 57K товаров) |

### Маппинг клиент↔авто

**Файл:** `client_cars_mapping.json` (в корне проекта)

**Логика:** Связи построены из истории заказ-нарядов 185.222:
- Клиент = Контрагент (кто платил)
- Авто = Автомобиль из заказа
- Одна машина может быть у нескольких клиентов (если разные люди платили)

**Как работает в TIPO-STO:**
1. При выборе клиента система проверяет `CLIENT_CARS_MAPPING[client_ref]`
2. Если есть — загружает эти авто
3. Если нет — ищет авто через историю заказов в Rent1C

**ВАЖНО:** При удалении дублей клиентов нужно переносить маппинг на оставшийся Ref!

```python
# Пример переноса маппинга
old_ref = "удалённый-ref"
new_ref = "оставшийся-ref"
mapping[new_ref] = mapping.pop(old_ref)
```

### История заказов

**Файл:** `order_history.json` (в корне проекта)

**Структура:**
```json
{
  "client_code": "ЦБ000140",
  "client_name": "Иванов Иван",
  "orders": [
    {
      "number": "USTS015378",
      "date": "2025-07-01",
      "sum": 2100,
      "car_name": "AUDI Q7 VIN WA1AJAF72MD01..."
    }
  ]
}
```

**Как работает в TIPO-STO:**
1. При загрузке клиента проверяется `ORDER_HISTORY[client_code]`
2. Исторические заказы добавляются к заказам из Rent1C
3. Статус исторических заказов = "История"
4. Сортировка по дате (новые сверху)

**Скрипт экспорта:** `/tmp/export_order_history.py` (запускать на 185.222)

### Детали заказов (работы + товары)

**Файл:** `order_details.json` (в корне проекта, 22MB)

**Статистика:**
- 25,185 заказов с деталями
- 60,020 позиций работ
- 57,424 позиций товаров

**Структура:**
```json
{
  "USTS004394": {
    "works": [
      {"name": "Замена лампы", "qty": 0.6, "price": 1000, "sum": 600}
    ],
    "goods": [
      {"name": "Лампа H7 Bosch 12v/55w PX26d", "qty": 1, "price": 500, "sum": 500}
    ]
  }
}
```

**API endpoint:** `GET /api/orders/history/{number}`

**Как работает в TIPO-STO:**
1. При клике на исторический заказ (статус "История")
2. Вызывается `/api/orders/history/{number}`
3. Возвращаются работы и товары из `ORDER_DETAILS[number]`

**Скрипт экспорта:** `/tmp/export_order_details.py` (запускать на 185.222)

### Скрипты миграции

Расположение: `/tmp/` (временные файлы на локальной машине)

**Экспорт с 185.222:**
- `export_works.json` — 2,928 работ
- `export_nomenclature.json` — 679,216 позиций номенклатуры (126MB)
- `export_prices.json` — 50,000 цен на запчасти
- `work_prices.json` — 2,495 цен на работы (из заказ-нарядов)

**Импорт в Rent1C:**
- `import_to_rent1c.py` — основной скрипт импорта
- `import_with_prices.py` — импорт номенклатуры только с ценами
- `update_work_prices.py` — обновление цен на работы

### Доступ к 185.222 для экспорта

```bash
# SSH
sshpass -p 'RhK312Sz1$' ssh 22Linia1@185.222.161.252

# 1C COM (требуется 32-bit Python)
C:\Python311-32\python.exe script.py

# Подключение к базе 1С
conn = win32com.client.Dispatch('V83.COMConnector')
db = conn.Connect('File="D:\\Base";Usr="Администратор";Pwd="12345678";')
```

### Особенности миграции

**Цены на работы:**
- В справочнике `Автоработы` нет поля цены
- Цены извлечены из табличной части `Работы` документов `ЗаказНаряд`
- Использовалась максимальная цена для каждой работы

**Номенклатура:**
- Полный объём: 679,216 записей
- Импортировано: 14,406 позиций (только те, что имеют цены)
- Остальные можно добавить по мере необходимости

**Пример тестовой заявки с мигрированными данными:**
```
Заявка №00000000031
Автомобиль: AUDI A4 № Х007НУ161
Работы:
  - Замена масла ДВС: ₽1,562.50
  - Замена ремня ГРМ: ₽9,500
  - Регулировка клапанов: ₽16,000
Запчасти:
  - Колодки тормозные: ₽2,500
  - Масло MOTUL 8100 5w-40: ₽2,500
ИТОГО: ₽32,062.50
```

---

## OData настройки в TIPO-STO

```python
ODATA_URL = "https://aclient.1c-hosting.com/1R96614/1R96614_AA61AS_e771ys34or/odata/standard.odata"
ODATA_USER = "Администратор"
ODATA_PASS = ""  # Пустой пароль
```

## Опубликованные объекты OData

- Catalog_Контрагенты
- Catalog_Автомобили
- Catalog_Организации
- Document_ЗаказНаряд
- И множество других (799 объектов)

## Файлы проекта

### Основное приложение (ИСПОЛЬЗУЕТСЯ)
- `/Users/khatlamadzieva/tipo-sto/demo_rent1c.py` - **Основной backend API**
- `/Users/khatlamadzieva/tipo-sto/demo_rent1c.html` - **UI интерфейс**

Запуск:
```bash
cd /Users/khatlamadzieva/tipo-sto
python demo_rent1c.py
# Открыть http://localhost:8000/
```

API эндпоинты:
- `/api/clients` - клиенты
- `/api/clients/{ref}` - детали клиента с авто и заказами
- `/api/cars` - автомобили
- `/api/orders` - список заказ-нарядов
- `/api/orders/{ref}` - детали заказ-наряда
- `/api/orders/create` - создание ЗаявкаНаРемонт
- `/api/orders/create_order` - создание ЗаказНаряд напрямую (с работами, товарами, исполнителями)
- `/api/orders/history/{number}` - детали исторического заказа из 185.222
- `/api/zayavka/{ref}/convert` - конвертация ЗаявкаНаРемонт в ЗаказНаряд
- `/api/ref/executors` - исполнители (69 сотрудников)
- `/api/ref/works` - работы с ценами и нормами времени
- `/api/ref/goods` - номенклатура (запчасти)
- `/api/ref/workshops` - цеха
- `/api/ref/employees` - все сотрудники
- `/api/ref/statuses` - состояния заказ-нарядов
- `/api/ref/repair_types` - виды ремонта
- `/api/stats` - статистика

### Архив
- `/Users/khatlamadzieva/tipo-sto/src/main.py` - старая версия (не используется)
- `demo_v2.html`, `test_server.py` - ранние версии

## Настройка публикации OData в 1С

Если нужно добавить/убрать объекты:
1. Войти в 1С: https://aclient.1c-hosting.com/1R96614/1R96614_AA61AS_e771ys34or
2. Администрирование → Интерфейсы для внешних систем
3. Настройка автоматического REST-сервиса → вкладка "Состав"
4. Загрузить метаданные → выбрать нужные объекты → Сохранить

---

## AI Помощник для механиков (январь 2026)

### Описание

AI-чат для механиков СТО на базе OpenAI GPT-4 Vision. Отвечает ТОЛЬКО на вопросы про автомобили, автозапчасти и работы СТО.

**Возможности:**
- 📷 Анализ фото автодеталей
- 🔧 Расшифровка OBD-II кодов ошибок (P0xxx, B0xxx, C0xxx, U0xxx)
- 🚗 Автоматическая расшифровка VIN-кода (марка, модель, тип)
- ⚙️ Моменты затяжки, процедуры ремонта
- 💡 Диагностика по симптомам

### VIN Декодер

Интегрирован **Auto.dev API** (1000 запросов/месяц бесплатно).

Когда механик отправляет VIN (17 символов) — бот автоматически расшифровывает:
```
🚗 Расшифровка VIN WBA25DP030N122357:
• Марка: BMW
• Модель: X3
• Тип: SUV
• Страна производства: Germany
```

### Доступ

- **URL:** http://198.12.73.168:8000/mechanic
- **API:** http://198.12.73.168:8000/api/assistant/chat

### Файлы

```
app/
├── api/
│   └── assistant.py      # API эндпоинты чата
├── services/
│   ├── ai_assistant.py   # Сервис OpenAI GPT-4 Vision
│   └── vin_decoder.py    # VIN декодер (Auto.dev API)
├── static/
│   └── mechanic.html     # UI чата для механиков
├── config.py             # Настройки (OPENAI_API_KEY, AUTODEV_API_KEY)
└── main.py               # FastAPI приложение
```

### API эндпоинты

| Эндпоинт | Метод | Описание |
|----------|-------|----------|
| `/api/assistant/chat` | POST | Отправить сообщение (+ image_base64 для фото) |
| `/api/assistant/status` | GET | Статус AI (настроен ли ключ) |
| `/api/assistant/logs` | GET | Логи чатов для анализа (?date=2026-01-27) |
| `/api/assistant/clear` | POST | Очистить историю сессии |

### Пример запроса

```bash
curl -X POST http://198.12.73.168:8000/api/assistant/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "какое масло залить в киа рио?", "session_id": "mechanic-1"}'
```

### Логирование чатов

Все чаты сохраняются в `data/chat_logs/YYYY-MM-DD.jsonl` для анализа:

```json
{
  "timestamp": "2026-01-27T11:04:32",
  "session_id": "mechanic-123",
  "message": "какое масло залить в киа рио?",
  "response": "Для Kia Rio рекомендуется 5W-30...",
  "has_image": false
}
```

### Настройка

В `.env` файле:
```
OPENAI_API_KEY=sk-proj-...
AUTODEV_API_KEY=sk_ad_...
```

### API лимиты

| Сервис | Лимит | Цена после лимита |
|--------|-------|-------------------|
| OpenAI GPT-4 Vision | По балансу | ~$0.01/запрос |
| Auto.dev VIN | 1000/месяц | $0.004/запрос |

### Ограничения

- Отвечает ТОЛЬКО на вопросы про автомобили и СТО
- На фото не-автодеталей отвечает: "Это не похоже на автозапчасть"
- VIN декодер не показывает год (API часто ошибается для европейских авто)

### Деплой на сервер

```bash
# Копировать файлы
sshpass -p 'S5M50Xb9Os2a8GvgUs' scp -o StrictHostKeyChecking=no -r app/ root@198.12.73.168:/root/tipoSTO/app/

# Перезапустить сервер
sshpass -p 'S5M50Xb9Os2a8GvgUs' ssh -o StrictHostKeyChecking=no root@198.12.73.168 "pkill -f 'uvicorn app.main'; sleep 1; cd /root/tipoSTO && nohup /root/tipoSTO/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /root/tipoSTO/app.log 2>&1 &"
```

### Сервер 198.12.73.168 (RackNerd)

Ubuntu 24.04 LTS. AI proxy (OpenAI-совместимый) работает на порту 8080.
TIPO-STO на порту 8000. Логи: `/root/tipoSTO/app.log`.
